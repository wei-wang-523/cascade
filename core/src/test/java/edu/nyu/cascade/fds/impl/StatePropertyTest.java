package edu.nyu.cascade.fds.impl;


import org.junit.Before;
import org.junit.Test;

public class StatePropertyTest {
  @Before
  public void setUp() throws Exception {
  }

  // Place-holder to avoid "no runnable tests" error
  @Test
  public void testNothing() { }

/*  @SuppressWarnings("unchecked")
  @Test
  public void testForall() {
    IStateVariable<IIntegerType> i = createMock(IStateVariable.class);
    expect(i.eq((IExpression<IIntegerType>) anyObject())).andAnswer(EasyMock.)
    i.eq(i).forall(i);
  }*/
}
