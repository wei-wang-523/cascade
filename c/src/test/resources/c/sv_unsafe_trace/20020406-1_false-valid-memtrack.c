
int deg(int f2) {
  return f2;
}

int exgcd(int f1) {
  int v, u;
  
  if (v != u)
    return exgcd(f1);
  
  while (v > 0)
  {
    int dv = deg(u);
  }
  return v;
}

int main() {
  int h;
  h = exgcd(h);
  return 0;
}
